Config { font = "xft:inconsolata:size=10:antialias=true"
       , bgColor = "#002b36"
       , fgColor = "#657b83"
       , position = Top
       , commands = [ Run Date "%a, %d %b %Y %T %Z" "date" 10
                    , Run StdinReader
                    ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%StdinReader% }{ <fc=#93a1a1>%date%</fc>"
       }
